#include <cmath>
#include <iostream>
#define PI 3.14
using namespace std;      //Совместимость с Windows
int main(char* args[])
{
	int x, y;
	cin >> x >> y; //Считывание
	cout << endl << "Resoult:" << pow(1-tan(x),1/tan(x)) + cos(x-y); //pow(число, степень)
	return 0;
}
